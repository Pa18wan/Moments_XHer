/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow these IPs to access the dev server
  allowedDevOrigins: ['10.215.32.26', '10.163.220.26', '192.168.137.1', 'localhost'],
};

export default nextConfig;
